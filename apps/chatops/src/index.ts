// Telegram chatops bot entrypoint. Placeholder for Phase 0 — the fixed
// command set (/status, /pause, /resume, /report) is built in Phase 5,
// calling the engine's internal API. MCP/natural-language handling comes
// after that, never before it (see root CLAUDE.md).
console.log("[chatops] scaffold ready — no bot wired up yet");
